import { ArrayField, NumberField, StringField } from '@schema-form-new/fields'
import SchemaForm from './SchemaForm'
import SelectionWidget from '@schema-form-new/widgets/Selection'
import ThemeProvider from './theme'

export default SchemaForm

export * from '../../types/types'

export { NumberField, StringField, ArrayField, SelectionWidget, ThemeProvider }
